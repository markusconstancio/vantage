"""In-app agents: recon (implemented), vuln-analysis, reporting, exploit.

Only recon is implemented so far. The exploit agent is intentionally absent
until its guardrails are reviewed — see docs/safety-framework.md rule 2.
"""
